// Code generated by goplus from filter.gp. DO NOT EDIT.
//goplus:v v0.28.0

package build

import (
	"fmt"

	"github.com/caarlos0/log"
	builders "goforge.dev/gpreleaser/pkg/build"
	"goforge.dev/gpreleaser/pkg/config"
	"goforge.dev/gpreleaser/pkg/context"
)

func filter(ctx *context.Context, build config.Build) []string {
	if !ctx.Partial {
		return build.Targets
	}
	target := ctx.PartialTarget
	fixer, ok := builders.For(build.Builder).(builders.TargetFixer)
	if ok {
		target = fixer.FixTarget(target)
	}
	log.WithField("match", fmt.Sprintf("target=%s", target)).Infof("partial build")

	var result []string
	for _, t := range build.Targets {
		if t != target {
			continue
		}
		result = append(result, t)
		break
	}
	return result
}
